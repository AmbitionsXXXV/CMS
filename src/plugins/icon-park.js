import { FullScreenOne } from '@icon-park/vue-next';
import { International } from '@icon-park/vue-next';
import { Theme } from '@icon-park/vue-next';
export default function IconPark (app) {
  app.component('FullScreenOne', FullScreenOne);
  app.component('International', International);
  app.component('Theme', Theme);

}